import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#ED6B1C"
        },
        secondary: {
            main: "#8CB8C9"
        }
    }
})

export default theme
